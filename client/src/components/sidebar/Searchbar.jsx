const SearchBar = () => {
  return (
    <div style={{ padding: "10px" }}>
      <input
        type="text"
        placeholder="Search or start a new chat"
        style={{ width: "100%" }}
      />
    </div>
  );
};

export default SearchBar;
